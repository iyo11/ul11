train:
  windows:
    datasets_path: "../datasets_local"
    batch_size: 8
    workers: 4
    cache: false

  linux:
    datasets_path: "../datasets"
    batch_size: 24
    workers: 10
    cache: true

  epochs: 200
  close_mosaic: 10
  model_name: "yolo11n_C3k2CGHalfConv_C2PSABinaryAttention.yaml"
  dataset_yaml: "/NWPU_VHR.yaml"
  seed: 42
  optimizer: "SGD"
  amp: true
  patience: 0
  pretrained: true
  module_edition: "e0"
